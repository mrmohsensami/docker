# docker
```bash
docker images
docker pull hello-world
docker run hello-world
docker ps
docker ps -a
```
```bash
docker run --name myname hello-world
docke rm myname
docker container prune
docker rmi hello-world
docker pull busybox
docker run --rm busybox
docker run busybox ls
docker run -it busybox
```
